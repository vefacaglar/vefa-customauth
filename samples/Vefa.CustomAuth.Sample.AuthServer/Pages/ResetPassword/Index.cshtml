@page
@model Vefa.CustomAuth.Sample.AuthServer.Pages.ResetPassword.IndexModel
@{
    ViewData["Title"] = "Update Password";
}

<div class="container">
    <div class="header">
        <h1>Update Password</h1>
        <div class="subtitle">enter your new password below</div>
    </div>

    @if (!string.IsNullOrEmpty(Model.ErrorMessage) && (string.IsNullOrEmpty(Model.Email) || string.IsNullOrEmpty(Model.Token)))
    {
        <div class="alert-error" style="text-align: center; flex-direction: column; align-items: center; justify-content: center; gap: 0.75rem;">
            <strong style="font-weight: 400; color: var(--text-primary);">Link Invalid or Expired</strong>
            <span style="font-size: 0.825rem; color: var(--text-secondary);">
                @Model.ErrorMessage
            </span>
        </div>

        <div class="auth-footer">
            <a href="/ForgotPassword?ReturnUrl=@System.Net.WebUtility.UrlEncode(Model.ReturnUrl ?? "")" class="auth-link">Request a New Link</a>
        </div>
    }
    else
    {
        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            <div class="alert-error">
                <span>@Model.ErrorMessage</span>
            </div>
        }

        <form method="post">
            <input type="hidden" name="Email" value="@Model.Email" />
            <input type="hidden" name="Token" value="@Model.Token" />
            <input type="hidden" name="ReturnUrl" value="@Model.ReturnUrl" />

            <div class="form-group">
                <label class="form-label" for="newPassword">New Password</label>
                <div class="input-wrapper">
                    <input class="input-control" id="newPassword" name="NewPassword" type="password" placeholder="Min. 4 characters" required autofocus />
                </div>
            </div>

            <div class="form-group">
                <label class="form-label" for="confirmNewPassword">Confirm New Password</label>
                <div class="input-wrapper">
                    <input class="input-control" id="confirmNewPassword" name="ConfirmNewPassword" type="password" placeholder="Confirm your new password" required />
                </div>
            </div>

            <button type="submit" class="btn-submit">Update Password</button>
        </form>

        <div class="auth-footer">
            <a href="/Login?ReturnUrl=@System.Net.WebUtility.UrlEncode(Model.ReturnUrl ?? "")" class="auth-link">Cancel and Sign In</a>
        </div>
    }
</div>
